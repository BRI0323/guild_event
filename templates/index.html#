<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campaign Management</title>
</head>
<body>
    <h1>Campaign Management</h1>

    <!-- 新しいキャンペーンの追加フォーム -->
    <form action="/add_campaign" method="post">
        <label for="name">Campaign Name:</label>
        <input type="text" name="name" required><br>
        <label for="total_pages">Total Pages:</label>
        <input type="number" name="total_pages" required><br>
        <label for="start_time">Start Time:</label>
        <input type="datetime-local" name="start_time" required><br>
        <label for="end_time">End Time:</label>
        <input type="datetime-local" name="end_time" required><br>
        <button type="submit">Add Campaign</button>
    </form>

    <h2>Current Campaigns</h2>
    <ul>
        {% for campaign in campaigns %}
            <li>
                {{ campaign.name }} - {{ campaign.total_pages }} pages 
                ({{ campaign.start_time }} to {{ campaign.end_time }})
                <a href="/delete_campaign/{{ campaign.id }}">Delete</a>
            </li>
        {% endfor %}
    </ul>
</body>
</html>
